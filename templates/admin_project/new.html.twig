{% extends 'base.html.twig' %}

{% block title %}New Project{% endblock %}

{% block body %}
	<turbo-frame id="project_new" >

		{{ form_start(form, {action: path('app_admin_project_new') }) }}
		{{ form_widget(form) }}
		<button class="btn" data-turbo-frame="_top">Modifier</button>
		{{ form_end(form) }}

	</turbo-frame>

{% endblock %}
