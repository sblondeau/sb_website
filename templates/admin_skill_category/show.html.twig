{% extends 'base.html.twig' %}

{% block title %}SkillCategory{% endblock %}

{% block body %}
    <h1>SkillCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill_category.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ skill_category.icon }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ skill_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_skill_category_index') }}">back to list</a>

    <a href="{{ path('app_admin_skill_category_edit', {'id': skill_category.id}) }}">edit</a>

    {{ include('admin_skill_category/_delete_form.html.twig') }}
{% endblock %}
