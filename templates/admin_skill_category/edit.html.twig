{% extends 'base.html.twig' %}

{% block title %}SB Admin | Catégories
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet"  href="{{ asset('styles/skills.css') }}">
	<link rel="stylesheet"  href="{{ asset('styles/admin.css') }}">
{% endblock %}

{% block body %}

	<turbo-frame id="category_{{ skill_category.id }}">
        <article class="article-edit">
		{{ component('SkillCollectionType', { skillCategory: skill_category }) }}
        </article>
	</turbo-frame>

{% endblock %}
